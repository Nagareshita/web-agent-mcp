# CLAUDE.md — プロジェクト憲法

> このファイルはClaude Codeが自動的に読み込む「プロジェクト憲法」です。
> すべてのセッションで常時コンテキストとして保持されます。

---

## 🌐 Web調査ルール（最重要）

**Web調査・情報検索が必要な場合は、必ず `web-agent` MCPサーバのツールを使用すること。**

- 組み込みの知識（学習済みデータ）だけで答えを推測しない
- ライブラリのバージョン、API仕様、最新情報はすべて `web-agent` で検索する
- ドキュメントURL・公式サイトを参照する場合も `web-agent` 経由でfetchする
- 「おそらく〜です」「〜だったはずです」といった不確かな回答をしない
- 調査結果は必ず出典URL付きで提示する

```
# MCPツール使用の優先順位
1. web-agent          ← Web検索・URL取得は必ずこれを使う
2. ファイル操作ツール  ← プロジェクト内ファイルの読み書き
3. bash               ← コマンド実行・ビルド・テスト
```

---

## 🛠️ 環境・ツール

```bash
# パッケージ管理（必ずuvを使う。pip直接実行禁止）
uv sync
uv run python ...
uv pip install <pkg> --break-system-packages  # 必要な場合のみ

# テスト
uv run pytest tests/ -x -v

# 型チェック
uv run mypy src/

# リント
uv run ruff check .
uv run ruff format .
```

---

## 📐 コーディング規約

- **言語**: Python 3.13
- **型ヒント**: 必須。`Any` の使用は原則禁止
- **docstring**: Google形式
- **非同期**: `async/await` を積極的に使う
- **エラーハンドリング**: 握りつぶし（`except: pass`）は絶対禁止

### 禁止事項

| 禁止 | 理由 |
|---|---|
| `conda` / `miniconda` | 商用ライセンス問題 |
| AGPL依存ライブラリ | 商用利用不可（例: PyMuPDF） |
| APIキーのハードコード | セキュリティポリシー違反 |
| `Any` 型の多用 | 型安全性の担保のため |
| `except: pass` | サイレント失敗は追跡不能 |

### 推奨ライセンス

`MIT` / `Apache 2.0` のみ依存可。不明な場合は `web-agent` で確認する。

---

## 🏗️ アーキテクチャ方針

- ドメイン固有のハードコード最適化は禁止（汎用化を優先）
- エージェント間のデータ契約は `Pydantic` モデルで定義する
- 並列ステートの競合は `Annotated` フィールドで解決する
- 設定はJSON/YAMLファイル駆動。コード内に直書きしない
- 認証は `DefaultAzureCredential`（Azure CLI認証）を使用。APIキー方式は使わない

---

## 📁 プロジェクト固有メモ

> ※ このセクションはプロジェクトに合わせて書き換えてください

```
# ポート番号
Qdrant     : 6333
Neo4j      : 7687
MCPサーバ  : 8101
LocalLLM   : <ポート番号を記載>

# 主要ディレクトリ
.agents/skills/   ← Agent Skills（Claude Code / Copilot / Cursor 互換）
src/              ← ソースコード
tests/            ← テスト
docs/             ← ドキュメント
```

---

## ✅ タスク開始前チェックリスト

新しいタスクを開始する前に以下を確認すること：

1. [ ] Web情報が必要な場合、`web-agent` MCPが使えるか確認
2. [ ] 依存ライブラリのライセンスを確認（MITまたはApache 2.0か）
3. [ ] 新規依存を追加する場合は `pyproject.toml` に追記
4. [ ] テストを書いてから実装する（TDDを推奨）
5. [ ] 型ヒントが正しく付いているか確認

---

## 🚫 Claudeへの明示的な禁止事項

- **推測で答えない**: 不確かな情報は必ず `web-agent` で確認してから回答する
- **コードを大量に書き換えない**: 変更は最小限に。大規模変更前は必ず確認する
- **テストなしで「動作します」と言わない**: 実際に実行して確認する
- **ファイルを無断で削除しない**: 削除前に必ず確認を取る
- **APIキー・シークレットをコードに書かない**: 環境変数か Azure Key Vault を使う

---

*このCLAUDE.mdはプロジェクトの成長に合わせて更新してください。*
*更新方法: Claudeに「このルールをCLAUDE.mdに追加して」と指示するだけでOK。*
